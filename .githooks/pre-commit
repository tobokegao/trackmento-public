#!/bin/sh
# 鍵を含みそうなものがコミットに入るのを、その場で止める。
#
# 入れ方（クローンごとに 1 回）:
#   git config core.hooksPath .githooks
#
# **判定そのものは scripts/scan_secrets.sh にある**（CI も同じものを呼ぶ。
# 2 か所に書くと必ずずれる）。ここはコミット時に呼ぶだけ。
#
# わざと入れたいとき（例示用の見本など）は `git commit --no-verify`。

exec sh scripts/scan_secrets.sh staged
