# 固定バージョン。Dependabot（.github/dependabot.yml）が毎週更新の PR を出し、audit.yml が脆弱性監査と起動テストをする。
# 手で上げるときは .venv で pip install -U した後に pip freeze で確認する。Docker は python:3.14-slim
fastapi==0.141.1
uvicorn[standard]==0.52.4
httpx==0.28.1
python-dotenv==1.2.3
pydantic==2.13.5
Pillow==12.3.0
beautifulsoup4==4.15.0
lxml==6.1.3
python-multipart==0.0.32
boto3==1.43.90          # Cloudflare R2（S3 互換）に共有 PNG を置くとき
