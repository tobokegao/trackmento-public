# 動画用メモ（v1 動画以降に増えた機能）

2026-09-10 19:16 に投稿した説明動画（v1）より後に実装したもの。次の動画で紹介する候補。
実装状況は 2026-09-14 時点。すべて本番（https://trackmento.onrender.com/）で動作確認済み。

---

## ■ 目玉：プレイリストを貼るだけで全部入る

- **プレイリストの URL を 1 本貼るだけで最大 500 曲がまとめて入る**
- 対応：ニコニコのマイリスト / YouTube の再生リスト / SoundCloud のセット /
  bilibili の収藏夹 / Spotify / Bandcamp / Apple Music
- 入れ方を選べる：**「マスに入れる」か「候補に置く」** をオーバーレイで選択
- マスに入れるときは **すでに埋まっているマスを飛ばして、若い番号から順に** 入る
- マス（最大 256）に入りきらない分は候補に残るので、そこから選んで絞り込める
- **複数の URL を改行で区切ってまとめて貼れる**。ニコニコと YouTube と Bandcamp を混ぜてもいい

デモ向きの素材：ニコニコの 500 件マイリスト `https://www.nicovideo.jp/mylist/79113711`
（本番で 484 件を約 3 秒で取得。ローカルだと 500 件）

## ■ 消えた動画がよみがえる

- プレイリストの中に **削除済み・非公開の動画** が混ざっていると、普通は
  「削除された動画」という灰色のマスになる
- TRACKMENTO は **otoDB（音MAD データベース）に問い合わせて、本当のタイトル・作者・
  サムネイルを復元する**
- otoDB のサムネイルは元動画が消えても残っているので成立する
- 動画 ID（`sm12345` など）を直接貼っても入る

上のマイリストで実演可能（消えた動画 9 件のうち 4 件が復元される）。

## ■ 英語対応（2026-09-14 追加）

- **右上のボタンで日本語 / 英語を切り替え**。選択は記憶され、初回はブラウザの言語で決まる
- 画面の文言 204 個すべてを翻訳済み
- **共有ページとエラーページも英語**。こちらは受け取った人のブラウザ設定（Accept-Language）で決まる
- 画面の文字を変えたら `scripts/check_i18n.py` を回す（英訳との対応が切れていないか見る）

## ■ マスが増えた・自由になった

- 上限 12×12 → **総数 256 マス**（16×16、12×20 のような **縦長・横長** も可）
- マスが多いときは **小さい画像を読みに行く** ので、256 マスでも重くならない

## ■ 探しやすくなった

- **英語でアーティスト名を入れても、日本語で登録されている曲が出る**（「Kenshi Yonezu」→ 米津玄師）
- Apple Music / iTunes の **URL 検索に対応**（曲・アルバム・プレイリスト）
- SoundCloud で曲にジャケットが無いときは **投稿者のアイコン** を使う
- ジャケットがどうしても無い曲には **TRACKMENTO のアイコン** が入る（新規描き下ろし）

## ■ 見た目（Mac OS 9 風）

- **ウィンドウバーを Mac OS 9（プラチナ）風のダサい縞模様に**
- **スクロールバーも Mac OS 9 風**。溝・つまみの立体感、つまみの真ん中の握り、矢印まで作り込み
  （握りはウィンドウバーの縞と同じ「白線＋2px 右下にずらした影線」）
- スマホのシート見出しを **計算機風の黒バー** に
- ボタン名を「共有データ出力/入力」→ **「並びを保存 / 並びを読み込み」** に変更
- アプリ内ブラウザだと **外部ブラウザを勧める案内帯** が出る
- PC 表示で候補ペインがグリッドと同じ高さに揃うようになった

## ■ 裏側（言わなくてもいいが、言えば効く）

- **サーバー代がモリモリ増えたので、転送量を徹底的に削った**
  - 日本語フォントを分割配信（1 訪問 210KB → ほぼゼロ）
  - Bandcamp のジャケット 1 枚 6.5MB → **19KB**
  - bilibili 640KB → 50KB、otoDB 166KB → **22KB**
  - 画像は 2 回目以降 Cloudflare R2 から直接配信
- 画質は落としていない。**配信元が用意している小さい版を選んでいるだけ**（otoDB だけ例外）
- Render の状態を **2 時間おきに自動点検** して異常があれば GitHub に Issue が立つ

---

## v2 の構成（2026-09-14 確定）

曲は **Sherbet.mp3**（`promo/public/sherbet.mp3`、2:31.9）。**BPM 130.00 ちょうど / 1 小節 1.846 秒 /
1 小節目の頭 = 0.52 秒**（頭の無音のあとの最初の音。2026-09-17 に全曲の櫛で測り直した。それまでの
「129.20・0.557 秒」は librosa の拍（40〜100ms 遅れ、末尾で 0.1 秒ずれる）から出した値で、
41〜42 小節のキメが 1 拍近く遅れていた）。拍は `promo/src/beats.json`（固定の格子、328 拍）。
測り方: scratchpad の `tempo.py` / `phase.py` 相当（ffmpeg でデコード → スペクトル流束 → 127〜133 を 0.01 刻みの櫛で当てる）。
**譜割りの共有エディタ** https://claude.ai/artifact/KNEULMAUpPGXSYdrCmWC1Q（場面・キメ・勢いを小節に乗せて書き込める。
共有ストレージ `plan/main` を Claude が直接読めるので、そこから timeline.ts に写す）
v1 の `theme.mp3` は 129.31 でテンポがほぼ同じなので、`SHOTS` の `speed` と尺はそのまま流用できる。
v1 の拍は `promo/src/beats-v1.json` に残してある。

**全 40 小節 = 74.3 秒**。0 秒（音の出だし）から使う。

| 小節 | 秒 | 内容 |
| --- | --- | --- |
| 1–3 | 0.0–5.6 | イントロ（音と同時にロゴ） |
| 4–8 | 5.6–14.9 | 画面説明 |
| 9 | 14.9–16.7 | **⑤ さらにダサくなった見た目** |
| 10 | 16.7–18.6 | 3, 2, 1, GO |
| 11 | 18.6–20.4 | まずはタイトル |
| 12–14 | 20.4–26.0 | 枠タップ／曲を探す／ソース 3 種 |
| 15 | 26.0–27.9 | URL 対応（7 サイトを 3 連で） |
| 16–17 | 27.9–31.6 | **① プレイリストをまとめて挿入** |
| 18–19 | 31.6–35.3 | **② 消えた動画の復活** |
| 20–24 | 35.3–44.6 | 手入力／全部埋まった／入れ替え／並べ終わった／ほぼ完成 |
| 25–26 | 44.6–48.3 | 出力設定／解像度 5 種 |
| 27–28 | 48.3–52.0 | **③ 256 マス** |
| 29–32 | 52.0–59.5 | 背景色／カスタム色／共有／JPG と URL・共有ページ |
| 33 | 59.5–61.3 | **④ 日英切替** |
| 34 | 61.3–63.2 | タイムラプス |
| 35–36 | 63.2–66.9 | できあがり |
| 37 | 66.9–68.7 | **⑥ 転送量削減** |
| 38–40 | 68.7–74.3 | エンドカード（ロゴ／URL／無料） |

- **①②を隣接させたのが肝**。別々に置くと無関係な機能に見えるが、並べると「リストを丸ごと入れる → その中の
  消えた動画も埋まる」という 4 小節ひと続きの話になる
- ⑤を画面説明の直後に置いたのは、見た目の話がそこで一番効くため。⑥はエンドカードの直前で Bandcamp 導線への助走

### 録画は本編と新機能の 2 本に分ける

① で 500 曲入れると 9 マスの流れが壊れるので、同じセッションでは撮れない。`record.mjs` に `SCENE=feat` を足して
`public/recordings-feat` に撮る。**英語版も作るので `MODE`（phone/pc）×`SCENE`（main/feat）×`LANG`（ja/en）で 8 本**。
英語は `#lang-switch` を押してから撮ると画面の文言まで英語になる。

feat の操作列（この順で 1 本に撮れる）:

| 割当 | 操作 | セレクタ |
| --- | --- | --- |
| ⑤ 見た目 | 候補を出してスクロール（OS 9 風バー） | `#q` → `#search-btn` → `#results` |
| ① プレイリスト | マイリスト URL → 「マスに入れる」 | `#bc-url` → `#bc-btn` → `#pl-modal` → `#pl-to-grid` |
| ② 復活 | `sm7482021` を貼って otoDB 復活 | `#bc-url` → `#bc-btn` |
| ③ 256 マス | 16×16 に変更 | `#cols` / `#rows` |
| ④ 日英 | 切替ボタン | `#lang-switch` |

### ⑤ の見くらべ（BEFORE / NOW）

前半 2 拍が v1 の画（`promo/public/v1-look{,-pc}.png`）、後半が今の録画。`Shot.ab` を付けたショットだけ
`BeforeAfter` で描く。v1 の画は **`record_v1look.mjs`** で撮る。

- **作業ツリーの `frontend/index.html` は触らない**。Playwright で「/」の応答だけを v1 の HTML に
  差し替える（route の横取り）。API も R2 も同じオリジンのまま使える
- v1 の HTML は `git show 7e38bc5~1:frontend/index.html`（Mac OS 9 風スクロールバーを入れる直前）。
  取り出したものは scratchpad に置く。サーバーが差し込む `<!--__FONT_LINK__-->` と `__RETENTION__` は
  スクリプトが自分で埋める
- **`deviceScaleFactor` を context に書く**。書かないと CSS px（1280×720）で撮れて、動画の枠に入れるとぼける。
  録画のときはフラグ（`--force-device-scale-factor`）で拡大しているが、`screenshot` はこちらを見る

**スクロールバーは v1 の画には映らない**（調べた結果）:
- スマホはタッチ端末扱いでスクロールバーが出ない。実機でも同じ
- v1 の PC 版は候補パネルが縦に伸びてページ全体がスクロールする作りで、パネルの中にバーが出ない。
  今の版は候補ペインをグリッドと同じ高さに揃える（`ce45b9c`）ので、パネルの中にバーが出る
- なので **NOW 側の録画（`look:scroll`）で動かして見せる**。見くらべ自体は画面の雰囲気の変化を担う

**`--hide-scrollbars` は feat では付けない**（`record.mjs`）。本編は余計なものを映さないよう付けたままだが、
feat は ⑤ でスクロールバーそのものを見せるので外している。

### ② に使う動画（2026-09-14 に実測で選定）

**`sm7889666`「しゃぶピーのグルメレース」/ 新ドナP**。**作品ごと消えている**（otoDB に登録された
ソースが 1 件で、それが削除済み）ので、貼ると otoDB からタイトル・作者・サムネが埋まる（`source=otodb`）。

- **波平のグルメレース本体は使えない**。otoDB に登録されたソースが YouTube（`72_dVdxlLkA`）1 件だけで、
  そちらは生きているため「復活」にならない（貼ると `source=youtube` で普通に取れる）。
  元はニコニコだが（説明欄「ニコより＜＾Ｖ＾＞」）、その sm ID は YouTube 側にも書かれておらず辿れない。
  **辿れたとしても otoDB に未登録なので roxy は引けない**（roxy がニコニコへ直接取りに行く経路は
  生きている動画にしか効かない）
- **「転載量がある」と「作品ごと消えている」は両立しない**。転載の多い作品は誰かが再アップし続けるので
  生き残り、作品ごと消えるのは元々 1 本しか上がっていないもの。グルメレース 147 件を全部調べて、
  作品ごと消えていて当たり障りがないのは `sm7889666` と `sm4682353`（馬鹿のグルメレース / ピアノ男）の 2 件だけだった
- 予備: `sm4682353`（馬鹿のグルメレース / ピアノ男、作品ごと消滅）。
  `sm7482021`（琴吹紬のグルメレース）は**その ID は消えているが再アップ `sm11006450` が生きている**ので避けた
- **otoDB に work として載っていても roxy が引けるとは限らない**。`sm6272432`（国崎のグルメレース）など
  3 件は登録済みなのに 400 だった。**素材は必ず `/from-url` に投げて `source=otodb` を確かめてから使う**

## v3 の構成（2026-09-17 確定）

v2（9/14）より後に増えたものを頭にまとめ、本編は v2 の小節をそのまま使う。曲は同じ Sherbet。

### 曲のキメ（利用者が書き出したもの。小節は v2 と同じ数え方 = 1 小節目が 0 秒）

| 小節 | 曲 |
| --- | --- |
| 1 | イントロ導入 |
| 2–9 | イントロの本格的な開始 |
| 10–17 | サビ 1 |
| 18–25 | サビ 1 のバージョン違いループ |
| 26–27 | 24–25 の反復で A メロを引き立てる |
| 28–42 | 4 小節ごとの A メロループ。**41 小節目 8 分音符 8 拍目、42 小節目 2 拍目・4 拍目でキメ**、6 拍目もキメだが次のサビまで音を伸ばす |
| 43–50 | サビ 2（さらに盛り上がる） |
| 51–58 | サビ 2 のバージョン違い。57 小節目 8 拍目、58 小節目 2・4 拍目でキメ、6 拍目は伸ばし |
| 59–66 | サビ 3 |
| 67–74 | サビ 3 のバージョン違い |
| 75–76 | 73–74 の反復でアウトロを引き立てる |
| 77 | アウトロ |

**v2 の並べ方はこの曲に合っていた**: 「並べる」28 が A メロの頭、「ほぼ完成」32 が 2 周目の頭、
**「できあがり」41–42 がキメ**、エンドカード 43 がサビ 2 の頭。v3 でも 28–42 は動かさない。

### 小節割り（47 小節 ≒ 87 秒）

| 小節 | 内容 | 録画 |
| --- | --- | --- |
| 1–3 | イントロ（音と同時にロゴ） | なし |
| 4–5 | **さらに軽くなりました**（v2 の ⑥ を新しい数字で。録画なしの画） | なし |
| 6–7 | **みんなのグリッド**: 「載せる」にチェックして共有 → 探すページで曲名を引くと他の人の並びが出る | feat |
| 8–9 | **パレット**: 8 色の組を切り替え（ナイトで画面ごと暗く）→ 自作の組を「コピー」で書き出し、貼って読み込む（組ごと人に渡せる） | feat |
| 10–11 | **曲名リストが賢く**（サビ 1 の頭）: 5×4 の回り込み／1×8 の柱／4×4 の高さいっぱい、を 3 連で。縦長・横長もここで見える | なし（`render.py` の静止画） |
| 12 | **英語ならローマ字**: 右上で EN に切り替えて検索 →「Marigold / Aimyon」 | feat |
| 13 | **VocaDB**: iTunes で「恐怖ガーデン」→ 0 件 → VocaDB に切り替え → ちゃぁ feat. 鏡音リンが出る | feat |
| 14 | **大きく見る**: 16×16 を開いて掴んで入れ替え（スマホはピンチ） | feat |
| 15–17 | トップ画面の説明（v2 の 5 小節を 3 小節に: これだけ／上にタイトル・真ん中にマス／下に共有と検索、出力設定） | main |
| 18–27 | 入れ方（v2 と同じ。20 の「検索ソースは 3 種類」→「4 種類」。26–27 の復活は曲の「引き立て」の 2 小節に乗る） | main / feat |
| 28–32 | 埋めて並べる（v2 と同じ。32 =「ほぼ完成です」） | main |
| 33–36 | 出力設定／比率 5 種／背景色（8 色）／カスタム色は HSV のつまみ | main |
| 37 | トラックを共有 → **送信の進み具合**（%・経過秒・作者の曲。押すのは小節の頭） | main |
| 38 | 画像と共有 URL | main |
| 39–40 | タイムラプス | main |
| 41–42 | **できあがり: 組み方の違う出力 4 枚をキメで切り替える**（41 の頭 → 41 の 8 拍目 → 42 の 2 拍目 → 4 拍目。6 拍目から 43 まで最後の 1 枚を伸ばす） | なし |
| 43 | **全部外す → 元に戻す**（「次を作るときは全部外す。間違えても戻せる」。サビ 2 の頭） | feat |
| 44–46 | エンドカード（ロゴ／URL／無料＋Bandcamp） | なし |
| 47 | 終わり（音楽は 44 からフェード） | |

- 頭の順は利用者の指定: 軽く → みんなのグリッド → パレット → 曲名リスト → ローマ字 → VocaDB → 大きく見る
- 送信の進み具合と全部外すは **1 小節ずつ**（2 拍ずつでは短い、と利用者）
- v2 の頭にあった翻訳・見た目の見くらべ・256 マスは外す。翻訳は 12 で、256 マスと縦長横長は 10–11・14 で映る。
  v1 との見くらべを残すなら 8 の前半 2 拍に v1 の画（`ab` の仕組みはそのまま）
- **VocaDB の曲は「恐怖ガーデン」ちゃぁ feat. 鏡音リン**（2026-09-17 に選定）。VocaDB の評価上位 100 曲を
  iTunes と突き合わせて、**サブスクに無い（iTunes 0 件）と確かめられた唯一の曲**。ほかの候補
  （リモコン・chaosmaid・BUTCHER VANITY）は題名だけで探すと本人名義の配信があった。
  VocaDB の検索で 1 位に出て、PV のサムネも付く

### 「さらに軽くなりました」に出す数字（v2 の 4 行と入れ替え）

- 1 アクセスあたりの転送量 90KB → 27KB（gzip、2026-09-16）
- 画面本体 273KB → 82KB
- 共有画像 501KB → 413KB（JPEG 0.82）
- 出来上がりの表示と保存は手元の画像（落とし直しゼロ）

### 録画の段取り

- v2 と同じ **main / feat × phone / pc × ja / en の 8 本**。`record.mjs` の feat の操作列に足すもの:
  「載せる」チェック → 共有 → `/find` で検索、パレットの切替とコピー／貼り付け、EN に切り替えて「マリーゴールド あいみょん」、
  VocaDB で「恐怖ガーデン」、大きく見るで 16×16、全部外す → 元に戻す
- 10–11 と 41–42 の静止画は `backend.render.render()` で作る（曲はボカロ 25 曲の一覧を繰り返して埋める。
  `promo/public/stills/*.png`、git 管理外）。**必ず `PUBLIC_MODE=1` で作る**（出力の最大辺が 2400 になる。
  付けないと割り付けが別物になり、文字が豆粒になる）。割り付けの規則を直したら作り直す
- 録画の印（`events.json` の `name`）は場面で分かれる: main の 8 本に `src:musicbrainz` … `bg:custom`、
  feat の 8 本に `listed:check` … `clear:tap`。`timeline.ts` が使う印がそろっているかは、各 `events.json` の
  `name` を集めて `timeline.ts` の `"x:y"` と突き合わせる（2026-09-17 は 8 本とも欠けなし）
- `/find` は同じサーバーで載せた共有がそのまま出る（手元は `PUBLIC_MODE=1 SHARE_LIMIT_PER_DAY=0`）

## 撮り方と書き出し方（2026-09-20 から。xxxbaaa さんの助言を反映）

X で xxxbaaa（@xxxbaaa07gk）さんから「音と画を合わせるのに足りないのは、フレーム単位の時計」
「本当の費用は、素材を 1 か所直したときに何回作り直すか」「AI に推測させず、決まった下書きから作る」と助言をもらった。
それに合わせて 3 つを作り直した。**古い record.mjs・record_all.sh・scan_markers.py・render_split.sh は消した**。

### 1. 1 コマずつ撮る（`promo/capture.mjs`）

- 以前は Playwright の録画（実時間・約 25fps・コマ落ちあり）を撮り、右下の色マーカーで操作の時刻を後から探して、
  ずれを 1 本の直線で補正していた。撮るたびに時刻が少しずつ変わる
- 今は**ページの時計を止めて**（Playwright の `page.clock`）、1/30 秒進めては 1 枚撮る。CSS のアニメーションも
  `document.getAnimations()` で同じ時刻に合わせる。操作の時刻は「何コマ目か」で記録するので、マーカー探しも速度の補正も要らない
  （`events.json` の `t` と `v` は同じ値になり、`timeline.ts` の `rate()` は 1 になる）
- 待ち（検索の結果など）は撮らずに待つ（`until`）。待つ様子を見せたいところ（ジャケットが読み込まれて埋まっていく・
  共有の送信の進み具合）だけ撮りながら待つ（`live`）
- 1 コマ 30ms ほどで撮れる。2 回撮って比べると、画素の値が 1 違うだけの同じ絵になる
- タップの波紋・PC のカーソルは前と同じ（`window.__tap`）。ホイールの代わりに自分でなめらかに送る（`scrollBy`。
  ブラウザのなめらかスクロールは実時間で動くため）。指で送るときは離す前に止める（はじいた勢いが実時間で動くため）

### 2. 場面ごとに撮り分ける

- 台本は場面（take）の並び。本編は `title` → `add-*`（9 曲）→ `reorder` → `options` → `share`、
  新機能は `multi` → `revive` → `author` → `listed` → `io` → `palette` → `vocadb` → `playlist` → `zoom` → `zoom32` → `pages`
- 各場面は**前の場面の終わりの localStorage** から始まる（`public/takes/<種類>/<場面>/end.json`）。
  入れるのはアプリの動いていないページ（`/health`）で行う（画面で入れると、読み込み直す前にページが自分の状態を保存し直して上書きする）。
  ブラウザの ID だけ新しくする（同じ ID だとサーバーの控えのほうが新しいと見なされる）
- **台本と頭の状態が前回と同じ場面は撮り直さない**（指紋は `meta.json`）。直した場面から後ろだけ撮り直す。
  撮り方そのものを変えたら `CAPTURE_VERSION` を上げる（全部撮り直し）
- 撮った場面をつないで `public/recordings*/session.mp4` と `events.json` を作る（Remotion はそのまま読む）。
  印には場面の名前（`take`）と指紋（`takeKey`）が付く
- **サーバーは本番と同じ見た目で立てる**: `PUBLIC_MODE=1 DISCOGS_TOKEN= PUBLIC_BASE_URL=https://trackmento.com
  SHARE_BUDGET_GB=0 SHARE_LIMIT_PER_DAY=0 SHARE_LIMIT_PER_IP_DAY=0`、ポート 8000。Discogs が出ていると撮影を止める
- **撮影で作った共有は本番の R2 に残る**（手元のサーバーも .env の R2 を使うため）。撮ったあと、`events.json` の
  `share-url` と `listed:shared` の ID を `scripts/compare_render.py clean <ID>…` で消し、`listed/<ID>.json` も消す
  （載せた共有は本番の「みんなのグリッドを探す」に出てしまう）
- 使い方: `node capture.mjs`（本編）、`SCENE=feat node capture.mjs`、`MODE=pc`・`LANG_UI=en` で横・英語。
  `--list` で撮り直しが要る場面を見る、`--only 名前,名前` で指定した場面だけ、`--force` で全部

### 3. 変わった区切りだけ書き出す（`promo/render_cached.mjs`）

- 2 小節ずつの区切りに分け、区切りごとの指紋（その区切りにかかるショットの設定・使う録画の場面の指紋・静止画・
  全体にかかるもの）が前回と同じなら `out/chunks/<Composition>/` の控えを使う。最後につないで音を付ける
- `node render_cached.mjs Promo out/v9-tall-ja.mp4`（`--dry` で描き直す区切りだけ見る、`--force` で全部）
- 束ね（bundle）は 1 回だけ作り、区切りごとに `remotion render` を呼ぶ（1 回ごとにプロセスが終わるのでメモリが溜まらない）

### 4. 台本はエディタから作る（`promo/plan_gen.mjs`）

- 場面の開始・長さ・字幕（日本語・英語）・録画の印・ずらし・速さは、**譜割りエディタの「場面」レーンの「動画の台本」欄にだけ置く**
  （種類: 録画 / エンドカードのあとの録画 / 作った画の各場面）。`timeline.ts` に数字を手で書き写さない
- Claude がエディタの DB（`plan/main`）を読んで `promo/plan.json` に保存 → `node plan_gen.mjs` で `src/plan.gen.ts` を作る
  （`--check` で食い違いだけ見る）。ズーム・枠・静止画などの見せ方の細かい値は `timeline.ts` の `SHOT_EXTRAS`（エディタの場面の ID ごと）
- エディタの「録画の印」欄は、撮った印の名前から選べる（DB の `evNames`。撮り直したら Claude が書き直す）

## v6 の構成（2026-09-19 深夜、利用者が譜割りエディタで組み直したもの）

v5 との違い（エディタの各場面のメモから）。

- 10 みんなのグリッド（1 小節）/ 11 探す → **見つかった共有ページの中まで開く**（`find:open`）
- 12 **並びを保存 → 全部外す → 読み込みで復活**（`io:*`。記事用 GIF「並びを保存／読み込み」と同じ流れ）
- 13 パレット / 14–15 自作パレット: コピー → 貼って読み込み → **ファイルに保存**（`pal:saved`）。
  保存した時刻に「ダウンロード」の窓を重ねる（`SavedWindow`、`Shot.explorer`）。**サイトにパレットのファイル保存を足した**（利用者の判断。無い機能を映さないため）
- 16 VocaDB は見つけた曲をマスに入れるまで。**プレイリストで 256 マスを埋める前に撮る**（埋めたあとだと入れる空きが無い）
- 17 曲名リストの静止画 16 枚は、スクラップブックのように角度と位置をばらして重ねる（`Shot.scrap`）
- 25 URL / 26 改行でまとめて / 27 プレイリスト / 31 手入力 / 32 ボカロの作者名は、**マスに入るところまで**
  （27 はマスが埋まるまで 15 秒あるので 8.8 倍で送る）。28 は入りきらなかった曲が候補の窓に並ぶところ（`pl:rest`）
- 33 大きく見るは **8×32 を縦に送る**（v5 は 32×1 を横に）
- 40–41 共有の送信（2 小節）。**上りを 80KB/秒に絞って撮る**（CDP の `Network.emulateNetworkConditions`）。
  手元のサーバーだと一瞬で 100% になり、進み具合のメーターが見えなかった
- 42.1 画像と共有 URL（3.5 拍）/ 42.4.5 タイムラプス（4.5 拍。キメは頭から 0・1・2・3 拍）
- 6–7・8–9 の点滅する一言は、4–5「新しい URL」と同じ字（本文の太字・墨）

## v5 の構成（2026-09-19 夜、利用者が譜割りエディタで組み直した 50 小節 ≒ 92 秒）

v4 との違いだけ書く（ほかは下の v4 の表と同じ）。ロゴの M と下線を差し替えたので書き出し直すついでに組み直した。

- 8–9 **共有がさらに速く**（作った画。`Bandwidth` を見出し・行・点滅の文を差し替えて使い回す。数字は `FASTER_ROWS`）
- 8–9 の「英語画面ならローマ字」と 32 の「マスを全部外す → 元に戻す」は外した（録画からも外した）
- 32 **ボカロの作者名も VocaDB から**（feat の `author:got`。Tell Your World sm17315575 は getthumbinfo に投稿者名が無く、
  VocaDB から「kz feat. 初音ミク」が入る）
- 49–50 **困ったら「更新情報」と「使い方」**（feat の `updates:page` / `guide:page`。エンドカードは 48 小節の終わりで消える。
  音楽は 46 小節からのフェードのまま）。`TAIL_SHOTS` を `shotSeq` で描く。**1 ショット 8 拍**（2 つに分けると字幕が 2 回出る）
- タイムラプスの最後のキメは横へ振らず、画を枠ごと横に 2 倍まで引き伸ばす
- 静止画（17 小節・44–45 小節）は曲名リストの直しに合わせて `make_stills.py` で作り直した
- 撮影で作った共有（`listed:shared`）は撮ったあと R2 から消した（本体・カード・並び・`listed/`）

## v4 の構成（2026-09-19 確定）

利用者が譜割りエディタ（https://claude.ai/artifact/KNEULMAUpPGXSYdrCmWC1Q、共有ストレージ `plan/main`）で決めた 49 小節（≒ 90 秒）。
曲の区切りとキメは聴き直して 1 小節後ろへ直した（A メロ 28–43、サビ 2 は 44 から、キメは 42 小節 4.5 拍〜43 小節）。
`promo/src/timeline.ts` の SHOTS が正本。エディタの位置表示は 1 始まりの「小節.拍.刻み」。

| 小節 | 場面 | 素材 |
| --- | --- | --- |
| 1–3 | イントロ | なし |
| 4–5 | 新しい URL は trackmento.com（旧 URL に取り消し線 → 矢印 → 新 URL、「並びごと引っ越し」） | 作った画（`NewUrl`）。**録画にはアドレス欄が映らないので録画にしない** |
| 6–7 | さらに軽くなりました | 作った画 |
| 8–9 / 10–11 / 12–13 | ローマ字 / みんなのグリッドに載せる / 曲名で探す | feat |
| 14 / 15 / 16 | パレット / コピー / VocaDB | feat |
| 17 | 曲名リストが賢く：**静止画 16 枚を 16 分音符ずつ** | `smart-01〜16`（`promo/make_stills.py`） |
| 18–20 | トップ画面の説明 | main |
| 21–31 | 入れ方〜手入力 | main / feat |
| 32 | マスを全部外す → 確認の窓 → 元に戻す | feat |
| 33–34 | 埋まったら / 入れ替え | main |
| 35 | 大きく見る：前半 2 拍 16×16、後半 2 拍 32×1 を横に送る | feat（`zoom:open` / `zoom32:swipe`） |
| 36 | 並べ終わったら（2 拍）/ ほぼ完成（2 拍） | main |
| 37 | 曲名リストは 3 択：「マスに重ねる」を押す（以後の共有は重ねる版のまま） | main（`list:overlay`） |
| 38–41 | 比率 / 背景色 / カスタム色 / 共有と送信の進み具合 | main |
| 42（3.5 拍） | 画像と共有 URL（trackmento.com で映す） | main |
| 42.4.5–43 | タイムラプス（4.5 拍、キメに乗る） | main |
| 44–45 | できあがり：4 枚を 2 拍ずつ（1 枚目 = 1 行型、4 枚目 = マスに重ねる） | `show-1〜4`（`promo/make_stills.py`） |
| 46–48 | エンドカード（ロゴ / trackmento.com / 無料＋Bandcamp） | なし |
| 49 | 終わり。**音楽は 46 小節からフェード** | なし |

- **早回し（speed）とズーム（zoomPc）はエディタで扱わない**（利用者が 2026-09-18 にレーンを削除）。v3 の場面ごとの値を使い、
  新しい場面は近い場面にならった値を入れた
- `beatTime` は拍の途中（0.25 拍など）も直線で補う（タイムラプスの頭と 17 小節の静止画のため）
- **録画は手元のサーバーで撮る**:
  `PUBLIC_MODE=1 DISCOGS_TOKEN= PUBLIC_BASE_URL=https://trackmento.com MIGRATE_TO= SHARE_BUDGET_GB=0 SHARE_LIMIT_PER_DAY=0 SHARE_LIMIT_PER_IP_DAY=0`。
  Discogs は本番に合わせて出さない。共有 URL の表示だけ trackmento.com になり、共有ページは手元の同じ ID を開く（`record.mjs`）
- `SCAN_PY` は `.venv` の python でよい（numpy が入った）
- **撮影で作った共有は本番の R2 に残る**（「みんなのグリッド」に載せた分は本番の探すページにも出る）。撮り終えたら消す
- 静止画: `PUBLIC_MODE=1 PYTHONUTF8=1 .venv/Scripts/python promo/make_stills.py`（曲は `promo/stills-tracks.json` に控えた 40 曲）

## 構成案

1. プレイリスト貼るだけ
2. 消えた動画も復活
3. 256 マス
4. 英語対応
5. Mac OS 9 風のバー・スクロールバー
6. サーバー代の話 ＋ Bandcamp リンク

## 撮影用の素材

- **`grids/default.json` は v1 動画（`promo/public/final.png`）と同じ並びに揃えてある**
  （9 曲・3x3・タイトル「私を構成する9選」）。v1 と同じ絵から始めて機能を見せる流れが作れる
  - `grids/*.bak` は古い状態（1 マス目が欠けている／ジャケットが違う）なので、そこから戻すと逆戻りする
  - **並びの正解は `promo/public/final.png` を見るのが早い**
- プレイリストのデモに使えるもの
  - ニコニコの 500 件マイリスト `https://www.nicovideo.jp/mylist/79113711`
    （本番で 484 件を約 3 秒。消えた動画 9 件のうち 4 件が otoDB から復元されるので、
    「消えた動画がよみがえる」もこれ 1 本で見せられる）
- **ローカルで撮るときは `PUBLIC_MODE=1` を付けて起動する**。付けないと
  `grids/default.json`（＝この並び）をテストで上書きしてしまう

## 制作メモ

- 動画は Remotion で作る予定。素材と手順は `promo/`（`src/timeline.ts` が構成、`src/Scenes.tsx` が描画）
- v1 の動画は未宣伝のプラットフォームに出し、v2（この内容）をあとで出す、という段取りだった
- v1 制作時の落とし穴は進捗メモリに残してある（Playwright の録画は実時間より 1 割遅い、
  `recordVideo` は CSS px で録るので `--force-device-scale-factor` で拡大、Sequence 内は相対フレーム）
